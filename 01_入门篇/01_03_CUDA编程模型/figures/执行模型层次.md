# CUDA执行模型层次结构

## Grid（网格）
- 包含多个Block
- 所有Block执行相同的kernel代码
- 可以是1D、2D或3D结构

## Block（线程块）
- 包含多个Thread
- Block内的Thread可以通过Shared Memory通信
- Block内的Thread可以同步（__syncthreads()）
- 可以是1D、2D或3D结构

## Thread（线程）
- GPU执行的最小单元
- 每个Thread有独立的寄存器
- 执行相同的kernel代码，但处理不同的数据（SIMT）